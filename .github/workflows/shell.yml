name: Shell Script

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run shell script
        run: |
              bash /home/kryan/gittest/.github/workflows/hello.sh
              chmod +x hello.sh
              ./hello.sh
